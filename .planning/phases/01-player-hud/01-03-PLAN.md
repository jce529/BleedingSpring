---
id: 01-03
phase: 1
wave: 2
depends_on: [01-01]
files_modified:
  - Assets/UI/PlayerVignette.cs
requirements_addressed: [HUD-04]
autonomous: true
---

# Plan 03: Red HP Vignette

## Objective
Create a Screen Space Overlay red vignette that pulses when the player's HP is below 25% (CurrentCleanWater / MaxCleanWater < 0.25).

## must_haves
- [ ] PlayerVignette.cs exists and compiles
- [ ] Vignette activates ONLY when HP ratio < 0.25
- [ ] Color is Red (#FF0000)
- [ ] Pulse speed is consistent (e.g., 1.5f)
- [ ] Vignette Image has Raycast Target = false
- [ ] Script subscribes/unsubscribes from PlayerWaterStats.OnWaterChanged
- [ ] Project compiles without errors

## Tasks

<task id="01-03-01">
<title>Create PlayerVignette MonoBehaviour script (Red HP version)</title>
<read_first>
- Assets/Player/PlayerWaterStats.cs — read event signatures
- .planning/phases/01-player-hud/01-CONTEXT.md — D-02, D-04
- .planning/phases/01-player-hud/01-RESEARCH.md — Sections 4, 5
</read_first>
<action>
Create/Overwrite Assets/UI/PlayerVignette.cs

```csharp
using UnityEngine;
using UnityEngine.UI;

public class PlayerVignette : MonoBehaviour
{
    [Header("References")]
    [SerializeField] private Image vignetteImage;
    [SerializeField] private PlayerWaterStats playerStats;

    [Header("Settings")]
    [SerializeField] private Color vignetteColor = Color.red;
    [SerializeField] private float pulseSpeed = 1.5f;
    [SerializeField] private float maxAlpha = 0.6f;

    private float _hpRatio;
    private static readonly Color ClearColor = new Color(0f, 0f, 0f, 0f);

    private void Start()
    {
        if (playerStats == null)
            playerStats = FindFirstObjectByType<PlayerWaterStats>();

        if (playerStats != null)
        {
            playerStats.OnWaterChanged += HandleWaterChanged;
            HandleWaterChanged(playerStats.CurrentCleanWater, playerStats.MaxCleanWater);
        }

        if (vignetteImage != null)
            vignetteImage.raycastTarget = false;
    }

    private void OnDestroy()
    {
        if (playerStats != null)
            playerStats.OnWaterChanged -= HandleWaterChanged;
    }

    private void HandleWaterChanged(float current, float max)
    {
        _hpRatio = (max > 0f) ? (current / max) : 0f;
    }

    private void Update()
    {
        if (vignetteImage == null) return;

        if (_hpRatio < 0.25f)
        {
            float t = Mathf.PingPong(Time.time * pulseSpeed, 1f);
            float alpha = t * maxAlpha;
            vignetteImage.color = new Color(vignetteColor.r, vignetteColor.g, vignetteColor.b, alpha);
        }
        else
        {
            vignetteImage.color = ClearColor;
        }
    }
}
```
</action>
<acceptance_criteria>
- Assets/UI/PlayerVignette.cs exists
- Vignette activates at HP ratio < 0.25
- Vignette pulses using `Mathf.PingPong`
- Raycast target is false
</acceptance_criteria>
</task>

## Verification
1. Enter Play Mode.
2. Reduce player HP below 25% (SacrificeWater or take damage).
3. Red vignette pulses on screen.
4. Heal HP above 25% — vignette disappears.
