---
phase: 04-player-stance-system
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - Assets/Player/BasicAttackSkill.cs
  - Assets/Player/WideSlashSkill.cs
  - Assets/Player/ProjectileSkill.cs
autonomous: false
requirements: [STN-03, STN-05]
must_haves:
  truths:
    - "WideSlashSkill의 SacrificeWater 호출에 costMultiplier가 곱해진다"
    - "ProjectileSkill의 SacrificeWater 호출에 costMultiplier가 곱해진다"
    - "모든 스킬의 데미지 계산에 effectMultiplier가 곱해진다"
    - "WideSlashSkill.CanUse()와 ProjectileSkill.CanUse()의 HP 체크에 costMultiplier가 반영된다"
    - "costMultiplier=1.0, effectMultiplier=1.0일 때 기존과 동일하게 동작한다 (하위 호환)"
  artifacts:
    - path: "Assets/Player/BasicAttackSkill.cs"
      provides: "effectMultiplier 적용된 데미지 계산"
      contains: "effectMultiplier"
    - path: "Assets/Player/WideSlashSkill.cs"
      provides: "costMultiplier + effectMultiplier 적용"
      contains: "costMultiplier"
    - path: "Assets/Player/ProjectileSkill.cs"
      provides: "costMultiplier + effectMultiplier 적용"
      contains: "costMultiplier"
  key_links:
    - from: "Assets/Player/WideSlashSkill.cs"
      to: "SkillBase.costMultiplier"
      via: "tsunamiHpCost * costMultiplier in SacrificeWater and CanUse"
      pattern: "tsunamiHpCost \\* costMultiplier"
    - from: "Assets/Player/ProjectileSkill.cs"
      to: "SkillBase.costMultiplier"
      via: "sniperHpCost * costMultiplier in SacrificeWater and CanUse"
      pattern: "sniperHpCost \\* costMultiplier"
    - from: "Assets/Player/BasicAttackSkill.cs"
      to: "SkillBase.effectMultiplier"
      via: "baseDamage * effectMultiplier in DamageBox calls"
      pattern: "baseDamage \\* effectMultiplier"
---

<objective>
각 구체 스킬 클래스에 costMultiplier와 effectMultiplier를 적용하여 보조 태세 스킬의 소모량/효과 감소가 실제로 동작하도록 한다. 기존 메인 슬롯 스킬은 기본값(1.0)으로 동일하게 동작해야 한다.

Purpose: STN-03(배율 적용)과 STN-05(하위 호환) 요구사항을 충족한다.
Output: 3개 스킬 파일 수정 + EDITOR-GUIDE.md 생성 (Inspector 설정 가이드)
</objective>

<execution_context>
@C:/Users/MSI/Projeect_A.E/My project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/Projeect_A.E/My project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-player-stance-system/04-CONTEXT.md
@.planning/phases/04-player-stance-system/04-RESEARCH.md
@.planning/phases/04-player-stance-system/04-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 SkillBase에 추가된 배율 필드 -->
From Assets/Player/SkillBase.cs (Plan 01 이후):
```csharp
[Header("태세 배율 (보조 태세용 인스턴스는 0.5 설정)")]
[SerializeField] protected float costMultiplier   = 1f;
[SerializeField] protected float effectMultiplier = 1f;
```

<!-- BasicAttackSkill 현재 구조 — SacrificeWater 호출 없음, DamageBox에서 baseDamage 사용 -->
From Assets/Player/BasicAttackSkill.cs:
```csharp
int hits = DamageBox(center, size, baseDamage);              // line 41 — 1차 타격
int echoHits = DamageBox(center, size, baseDamage * 0.5f);   // line 49 — 3단계 잔상
private int DamageBox(Vector2 center, Vector2 size, float damage)
{
    var hits = Physics2D.OverlapBoxAll(center, size, 0f, enemyLayer);
    foreach (var h in hits)
        h.GetComponent<IDamageable>()?.TakeDamage(damage, GetCorruptionDamage(damage));
    return hits.Length;
}
```

<!-- WideSlashSkill 현재 SacrificeWater + 데미지 위치 -->
From Assets/Player/WideSlashSkill.cs:
```csharp
// CanUse (line 33-40):
if (Stage >= 3 && Context.Stats.CurrentCleanWater <= tsunamiHpCost)

// ExecuteSlash (line 58):
float damage = baseDamage * DamageMult[stage];

// ExecuteTsunamiSlash (line 92):
Context.Stats.SacrificeWater(tsunamiHpCost);
// (line 102):
float dmg = baseDamage * DamageMult[3];
```

<!-- ProjectileSkill 현재 SacrificeWater + 데미지 위치 -->
From Assets/Player/ProjectileSkill.cs:
```csharp
// CanUse (line 29-33):
if (Stage >= 3 && Context.Stats.CurrentCleanWater <= sniperHpCost)

// ExecuteSkill case 1 (line 47):
FireProjectile(baseDamage, stage1Speed, penetrating: false);

// ExecuteSkill case 2 (line 52):
FireProjectile(baseDamage * 1.3f, stage2Speed, penetrating: false);

// FireLaser (line 86):
Context.Stats.SacrificeWater(sniperHpCost);
// (line 97):
h.GetComponent<IDamageable>()?.TakeDamage(sniperDamage, GetCorruptionDamage(sniperDamage));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3개 스킬 클래스에 costMultiplier / effectMultiplier 적용</name>
  <files>Assets/Player/BasicAttackSkill.cs, Assets/Player/WideSlashSkill.cs, Assets/Player/ProjectileSkill.cs</files>
  <read_first>
    - Assets/Player/BasicAttackSkill.cs (DamageBox 호출 위치, 3단계 잔상 로직)
    - Assets/Player/WideSlashSkill.cs (CanUse 조건, ExecuteSlash의 damage 계산, ExecuteTsunamiSlash의 SacrificeWater + dmg 계산)
    - Assets/Player/ProjectileSkill.cs (CanUse 조건, ExecuteSkill case 1/2의 FireProjectile 인수, FireLaser의 SacrificeWater + sniperDamage)
    - Assets/Player/SkillBase.cs (costMultiplier, effectMultiplier 필드 위치 확인 — Plan 01 완료 후)
    - .planning/phases/04-player-stance-system/04-RESEARCH.md (Pattern 2, Pattern 3, 함정 3, 함정 4)
  </read_first>
  <action>
**중요 원칙:** 각 파일에서 수정하는 줄만 정확히 변경하고, 나머지 코드/주석/포맷팅은 일체 건드리지 않는다.

## BasicAttackSkill.cs — effectMultiplier 적용 (2곳)

**수정 1 — ExecuteSkill() 내 1차 타격 (현재 line 41 부근):**
```csharp
// 변경 전: int hits = DamageBox(center, size, baseDamage);
// 변경 후:
int hits = DamageBox(center, size, baseDamage * effectMultiplier);
```

**수정 2 — ExecuteSkill() 내 3단계 잔상 (현재 line 49 부근):**
```csharp
// 변경 전: int echoHits = DamageBox(center, size, baseDamage * 0.5f);
// 변경 후:
int echoHits = DamageBox(center, size, baseDamage * effectMultiplier * 0.5f);
```

**수정 3 — Debug.Log의 데미지 표시 (현재 line 38 부근):**
```csharp
// 변경 전: $"데미지: {baseDamage:F0}"
// 변경 후:
$"데미지: {baseDamage * effectMultiplier:F0}"
```

SacrificeWater 호출이 없으므로 costMultiplier 적용 대상 아님. DamageBox 내부의 GetCorruptionDamage(damage)는 이미 effectMultiplier가 적용된 damage를 받으므로 자동으로 비례 적용된다 (함정 4 해결).

## WideSlashSkill.cs — costMultiplier + effectMultiplier 적용 (4곳)

**수정 1 — CanUse() HP 체크 (현재 line 35):**
```csharp
// 변경 전: if (Stage >= 3 && Context.Stats.CurrentCleanWater <= tsunamiHpCost)
// 변경 후:
if (Stage >= 3 && Context.Stats.CurrentCleanWater <= tsunamiHpCost * costMultiplier)
```

**수정 2 — ExecuteSlash() 데미지 계산 (현재 line 58):**
```csharp
// 변경 전: float damage = baseDamage * DamageMult[stage];
// 변경 후:
float damage = baseDamage * DamageMult[stage] * effectMultiplier;
```

**수정 3 — ExecuteTsunamiSlash() SacrificeWater (현재 line 92):**
```csharp
// 변경 전: Context.Stats.SacrificeWater(tsunamiHpCost);
// 변경 후:
Context.Stats.SacrificeWater(tsunamiHpCost * costMultiplier);
```

**수정 4 — ExecuteTsunamiSlash() 데미지 (현재 line 102):**
```csharp
// 변경 전: float dmg = baseDamage * DamageMult[3];
// 변경 후:
float dmg = baseDamage * DamageMult[3] * effectMultiplier;
```

## ProjectileSkill.cs — costMultiplier + effectMultiplier 적용 (5곳)

**수정 1 — CanUse() HP 체크 (현재 line 30):**
```csharp
// 변경 전: if (Stage >= 3 && Context.Stats.CurrentCleanWater <= sniperHpCost)
// 변경 후:
if (Stage >= 3 && Context.Stats.CurrentCleanWater <= sniperHpCost * costMultiplier)
```

**수정 2 — ExecuteSkill() case 1 (현재 line 47):**
```csharp
// 변경 전: FireProjectile(baseDamage, stage1Speed, penetrating: false);
// 변경 후:
FireProjectile(baseDamage * effectMultiplier, stage1Speed, penetrating: false);
```

**수정 3 — ExecuteSkill() case 2 (현재 line 52):**
```csharp
// 변경 전: FireProjectile(baseDamage * 1.3f, stage2Speed, penetrating: false);
// 변경 후:
FireProjectile(baseDamage * 1.3f * effectMultiplier, stage2Speed, penetrating: false);
```

**수정 4 — FireLaser() SacrificeWater (현재 line 86):**
```csharp
// 변경 전: Context.Stats.SacrificeWater(sniperHpCost);
// 변경 후:
Context.Stats.SacrificeWater(sniperHpCost * costMultiplier);
```

**수정 5 — FireLaser() sniperDamage (현재 line 97):**
```csharp
// 변경 전: h.GetComponent<IDamageable>()?.TakeDamage(sniperDamage, GetCorruptionDamage(sniperDamage));
// 변경 후:
float laserDmg = sniperDamage * effectMultiplier;
h.GetComponent<IDamageable>()?.TakeDamage(laserDmg, GetCorruptionDamage(laserDmg));
```
sniperDamage는 baseDamage와 별도 필드이므로 반드시 effectMultiplier를 별도 적용해야 한다 (RESEARCH.md에서 명시된 함정).

**수정 6 — FireLaser() Debug.Log의 데미지 표시도 업데이트:**
```csharp
// 변경 전: $"데미지: {sniperDamage:F0}"
// 변경 후:
$"데미지: {sniperDamage * effectMultiplier:F0}"
```

**하위 호환 검증:** costMultiplier=1f, effectMultiplier=1f (기본값)일 때 모든 계산은 `값 * 1f = 값`이므로 기존과 동일하게 동작한다.
  </action>
  <verify>
    <automated>grep -n "costMultiplier" Assets/Player/WideSlashSkill.cs Assets/Player/ProjectileSkill.cs && grep -n "effectMultiplier" Assets/Player/BasicAttackSkill.cs Assets/Player/WideSlashSkill.cs Assets/Player/ProjectileSkill.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Player/WideSlashSkill.cs에 `tsunamiHpCost * costMultiplier` 문자열이 2회 존재한다 (CanUse + SacrificeWater)
    - Assets/Player/WideSlashSkill.cs에 `* effectMultiplier` 문자열이 2회 존재한다 (ExecuteSlash + ExecuteTsunamiSlash)
    - Assets/Player/ProjectileSkill.cs에 `sniperHpCost * costMultiplier` 문자열이 2회 존재한다 (CanUse + SacrificeWater)
    - Assets/Player/ProjectileSkill.cs에 `* effectMultiplier` 문자열이 3회 이상 존재한다 (case 1 + case 2 + FireLaser)
    - Assets/Player/BasicAttackSkill.cs에 `baseDamage * effectMultiplier` 문자열이 존재한다
    - Assets/Player/BasicAttackSkill.cs에 `baseDamage * effectMultiplier * 0.5f` 문자열이 존재한다
    - Assets/Player/BasicAttackSkill.cs에 `costMultiplier` 문자열이 존재하지 않는다 (SacrificeWater 호출 없으므로)
    - 각 파일의 클래스 선언, 필드 선언, 기존 메서드 시그니처가 변경되지 않았다
  </acceptance_criteria>
  <done>3개 스킬 클래스에 costMultiplier/effectMultiplier가 정확한 위치에 적용되었고, 기본값 1.0에서 기존 동작과 동일하다</done>
</task>

<task type="auto">
  <name>Task 2: EDITOR-GUIDE.md 생성 (Inspector 설정 가이드)</name>
  <files>.planning/phases/04-player-stance-system/EDITOR-GUIDE.md</files>
  <read_first>
    - .planning/phases/04-player-stance-system/04-CONTEXT.md (Inspector 세팅 예시 섹션)
    - .planning/phases/04-player-stance-system/04-RESEARCH.md (Anti-Patterns, Inspector 배열 크기 초기화)
    - .claude/skills/unity-editor-guide/SKILL.md (EDITOR-GUIDE 표준 양식)
  </read_first>
  <action>
`.planning/phases/04-player-stance-system/EDITOR-GUIDE.md` 파일을 생성한다. unity-editor-guide 스킬의 표준 양식을 따른다.

내용에 포함할 항목:

**Hierarchy Setup:**
- Player GameObject에 PlayerStanceManager 컴포넌트 추가 (Add Component)
- Player GameObject에 보조 태세용 스킬 컴포넌트 추가: WideSlashSkill (Sub), ProjectileSkill (Sub), BasicAttackSkill (Sub) — 각각 별도 인스턴스

**Inspector Settings 테이블:**
| Object | Component | Property | Value / Action |
|--------|-----------|----------|----------------|
| Player | PlayerStanceManager | Main Stance | Water (드롭다운) |
| Player | PlayerStanceManager | Sub Stance | Water (드롭다운) |
| Player | PlayerStanceManager | Main Skills [0] | 기존 BasicAttackSkill 컴포넌트 드래그 |
| Player | PlayerStanceManager | Main Skills [1] | 기존 WideSlashSkill 컴포넌트 드래그 |
| Player | PlayerStanceManager | Main Skills [2] | 기존 ProjectileSkill 컴포넌트 드래그 |
| Player | PlayerStanceManager | Sub Skills [0] | 새로 추가한 BasicAttackSkill (Sub) 드래그 |
| Player | PlayerStanceManager | Sub Skills [1] | 새로 추가한 WideSlashSkill (Sub) 드래그 |
| Player | PlayerStanceManager | Sub Skills [2] | 새로 추가한 ProjectileSkill (Sub) 드래그 |
| Player | BasicAttackSkill (Sub) | Cost Multiplier | 0.5 |
| Player | BasicAttackSkill (Sub) | Effect Multiplier | 0.5 |
| Player | WideSlashSkill (Sub) | Cost Multiplier | 0.5 |
| Player | WideSlashSkill (Sub) | Effect Multiplier | 0.5 |
| Player | ProjectileSkill (Sub) | Cost Multiplier | 0.5 |
| Player | ProjectileSkill (Sub) | Effect Multiplier | 0.5 |
| Player | BasicAttackSkill (기존 Main) | Cost Multiplier | 1.0 (기본값 유지) |
| Player | BasicAttackSkill (기존 Main) | Effect Multiplier | 1.0 (기본값 유지) |

**Reference Connections 주의사항:**
- 같은 타입의 컴포넌트가 2개 존재할 때 Inspector에서 올바른 인스턴스를 드래그해야 한다
- GetComponent는 첫 번째만 반환하므로 반드시 Inspector 직렬화 참조를 사용한다
- 보조 스킬 컴포넌트의 Enemy Layer 마스크도 메인과 동일하게 설정해야 한다

**Manual Test (Play Mode):**
- Play Mode 진입 후 Console에 NullReferenceException 없음 확인
- 보조 슬롯 스킬의 costMultiplier=0.5 상태에서 Debug.Log로 HP 소모량이 메인의 절반인지 확인
- 메인 슬롯 스킬(costMultiplier=1.0)이 기존과 동일한 데미지/소모량인지 확인
  </action>
  <verify>
    <automated>grep -n "EDITOR-GUIDE" .planning/phases/04-player-stance-system/EDITOR-GUIDE.md > /dev/null 2>&1 && echo "EDITOR-GUIDE exists" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/04-player-stance-system/EDITOR-GUIDE.md 파일이 존재한다
    - 파일에 `PlayerStanceManager` 문자열이 존재한다
    - 파일에 `costMultiplier` 및 `effectMultiplier` 문자열이 존재한다
    - 파일에 `0.5` 값이 보조 태세 설정으로 명시되어 있다
    - 파일에 `Manual Test` 또는 `Play Mode` 섹션이 존재한다
  </acceptance_criteria>
  <done>EDITOR-GUIDE.md가 생성되어 Inspector에서 PlayerStanceManager와 보조 스킬 컴포넌트를 설정하는 방법이 문서화되었다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Play Mode 수동 검증</name>
  <what-built>
Plan 01에서 태세 시스템 인프라(StanceType enum, PlayerStanceManager, SkillBase 배율 필드, IPlayerContext 확장, PlayerController 연동)를 구축했고, Plan 02 Task 1에서 3개 스킬 클래스에 costMultiplier/effectMultiplier를 적용했다.
  </what-built>
  <how-to-verify>
1. Unity 편집기에서 프로젝트를 열고 컴파일 오류가 없는지 확인한다
2. EDITOR-GUIDE.md를 따라 Player GameObject에 PlayerStanceManager와 보조 스킬 컴포넌트를 설정한다
3. Play Mode 진입:
   - Console에 NullReferenceException이 없는지 확인
   - 메인 슬롯 스킬(costMultiplier=1.0) 사용 시 기존과 동일한 데미지/HP 소모량 확인
   - 보조 슬롯 스킬(costMultiplier=0.5) 사용 시 HP 소모량이 메인의 절반인지 Debug.Log로 확인
   - 보조 슬롯 스킬(effectMultiplier=0.5) 사용 시 데미지가 메인의 절반인지 확인
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 문제 설명</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "costMultiplier\|effectMultiplier" Assets/Player/WideSlashSkill.cs Assets/Player/ProjectileSkill.cs Assets/Player/BasicAttackSkill.cs` — 모든 적용 위치 확인
- `grep -c "costMultiplier" Assets/Player/BasicAttackSkill.cs` — 0이어야 함 (SacrificeWater 없으므로)
- `grep -c "effectMultiplier" Assets/Player/BasicAttackSkill.cs` — 2 이상이어야 함 (1차 + 잔상)
- Unity Play Mode에서 메인 스킬 동작 정상 (하위 호환 STN-05)
- Unity Play Mode에서 보조 스킬 소모/데미지 50% 감소 (STN-03)
</verification>

<success_criteria>
1. WideSlashSkill.cs의 SacrificeWater, CanUse, 데미지 계산 모두에 배율이 적용되었다
2. ProjectileSkill.cs의 SacrificeWater, CanUse, 모든 데미지(baseDamage + sniperDamage) 계산에 배율이 적용되었다
3. BasicAttackSkill.cs의 1차 타격과 3단계 잔상 데미지에 effectMultiplier가 적용되었다
4. 기본값(1.0)에서 기존 동작과 100% 동일하다 (하위 호환)
5. EDITOR-GUIDE.md가 Inspector 설정을 완전히 안내한다
6. Play Mode 수동 검증에서 보조 태세 스킬의 50% 감소 동작이 확인되었다
</success_criteria>

<output>
After completion, create `.planning/phases/04-player-stance-system/04-02-SUMMARY.md`
</output>
