---
phase: 04-player-stance-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Player/StanceType.cs
  - Assets/Player/PlayerStanceManager.cs
  - Assets/Player/SkillBase.cs
  - Assets/Player/IPlayerContext.cs
  - Assets/Player/PlayerController.cs
autonomous: true
requirements: [STN-01, STN-02, STN-03, STN-04]
must_haves:
  truths:
    - "StanceType enum이 정의되어 코드에서 태세 종류를 식별할 수 있다"
    - "PlayerStanceManager가 메인/보조 태세와 각 3개 SkillBase 슬롯을 Inspector에서 할당할 수 있다"
    - "SkillBase에 costMultiplier와 effectMultiplier 필드(기본값 1.0)가 존재한다"
    - "IPlayerContext.StanceManager 프로퍼티로 스킬에서 태세 정보를 참조할 수 있다"
    - "PlayerController.Start()에서 보조 스킬 Initialize가 호출된다"
  artifacts:
    - path: "Assets/Player/StanceType.cs"
      provides: "StanceType enum 정의"
      contains: "public enum StanceType"
    - path: "Assets/Player/PlayerStanceManager.cs"
      provides: "태세 상태 관리 컴포넌트"
      exports: ["Initialize", "GetMainSkill", "GetSubSkill", "SetStances"]
    - path: "Assets/Player/SkillBase.cs"
      provides: "costMultiplier, effectMultiplier 필드"
      contains: "protected float costMultiplier"
    - path: "Assets/Player/IPlayerContext.cs"
      provides: "StanceManager 프로퍼티"
      contains: "PlayerStanceManager StanceManager"
    - path: "Assets/Player/PlayerController.cs"
      provides: "StanceManager 구현 및 보조 스킬 초기화"
      contains: "StanceManager?.Initialize"
  key_links:
    - from: "Assets/Player/PlayerController.cs"
      to: "Assets/Player/PlayerStanceManager.cs"
      via: "GetComponent<PlayerStanceManager>() in Awake, Initialize(this) in Start"
      pattern: "GetComponent<PlayerStanceManager>"
    - from: "Assets/Player/PlayerStanceManager.cs"
      to: "Assets/Player/SkillBase.cs"
      via: "subSkills[].Initialize(context) in Initialize()"
      pattern: "s\\.Initialize\\(context\\)"
    - from: "Assets/Player/IPlayerContext.cs"
      to: "Assets/Player/PlayerStanceManager.cs"
      via: "StanceManager property type"
      pattern: "PlayerStanceManager StanceManager"
---

<objective>
태세 시스템의 기반 인프라를 구축한다: StanceType enum, PlayerStanceManager 컴포넌트, SkillBase 배율 필드, IPlayerContext 확장, PlayerController 연동.

Purpose: 메인/보조 태세 데이터 구조와 스킬 배율 인프라를 확립하여, Plan 02에서 각 스킬에 배율을 적용할 수 있는 토대를 만든다.
Output: 5개 파일 (2개 신규, 3개 수정) — 컴파일 오류 없이 Unity 편집기에서 로드 가능.
</objective>

<execution_context>
@C:/Users/MSI/Projeect_A.E/My project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/Projeect_A.E/My project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-player-stance-system/04-CONTEXT.md
@.planning/phases/04-player-stance-system/04-RESEARCH.md

<interfaces>
<!-- 현재 IPlayerContext 인터페이스 (수정 대상) -->
From Assets/Player/IPlayerContext.cs:
```csharp
public interface IPlayerContext
{
    bool             FacingRight  { get; }
    Rigidbody2D      Rigidbody    { get; }
    PlayerWaterStats Stats        { get; }
    PlayerState      CurrentState { get; }
    void ChangeState(PlayerState newState);
    void SetInvincible(bool value);
}
```

<!-- SkillBase.Initialize 시그니처 (보조 스킬 초기화에 사용) -->
From Assets/Player/SkillBase.cs:
```csharp
public void Initialize(IPlayerContext context)
{
    Context = context;
    Anim    = GetComponent<Animator>();
    OnInitialize();
}
```

<!-- PlayerController.Awake/Start 구조 (수정 대상) -->
From Assets/Player/PlayerController.cs:
```csharp
private void Awake()
{
    Rigidbody    = GetComponent<Rigidbody2D>();
    Stats        = GetComponent<PlayerWaterStats>();
    movement     = GetComponent<PlayerMovement>();
    inputHandler = GetComponent<InputHandler>();
    basicAttack = GetComponent<BasicAttackSkill>()  as ISkill;
    wideSlash   = GetComponent<WideSlashSkill>()    as ISkill;
    projectile  = GetComponent<ProjectileSkill>()   as ISkill;
    TryGetComponent(out animator);
}

private void Start()
{
    movement.Initialize(this);
    InitSkill(basicAttack);
    InitSkill(wideSlash);
    InitSkill(projectile);
    // ... event subscriptions
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StanceType enum + PlayerStanceManager + SkillBase 배율 필드</name>
  <files>Assets/Player/StanceType.cs, Assets/Player/PlayerStanceManager.cs, Assets/Player/SkillBase.cs</files>
  <read_first>
    - Assets/Player/SkillBase.cs (현재 필드 구조와 Header 위치 확인)
    - Assets/Player/IPlayerContext.cs (Initialize 파라미터 타입 확인)
    - .planning/phases/04-player-stance-system/04-RESEARCH.md (Pattern 1, Pattern 4, Anti-Patterns 확인)
  </read_first>
  <action>
**1. StanceType.cs 신규 생성** (Assets/Player/StanceType.cs):
```csharp
public enum StanceType
{
    None  = 0,
    Water = 1,
}
```
파일 전체가 이 enum 정의뿐이다. namespace 없음 (프로젝트 기존 패턴 준수).

**2. PlayerStanceManager.cs 신규 생성** (Assets/Player/PlayerStanceManager.cs):
- `using UnityEngine;` 및 `using UnityEngine.Assertions;` 사용
- `public class PlayerStanceManager : MonoBehaviour`
- 프로퍼티: `public StanceType MainStance { get; private set; }`, `public StanceType SubStance { get; private set; }`
- 필드: `[SerializeField] private SkillBase[] mainSkills = new SkillBase[3];`, `[SerializeField] private SkillBase[] subSkills = new SkillBase[3];`
- `public void Initialize(IPlayerContext context)` 메서드: `foreach (var s in subSkills) if (s != null) s.Initialize(context);`
- `public ISkill GetMainSkill(int slot)` 메서드: `Assert.IsTrue(slot >= 0 && slot < mainSkills.Length, ...)` 후 `return mainSkills[slot];`
- `public ISkill GetSubSkill(int slot)` 메서드: 동일 패턴
- `public void SetStances(StanceType main, StanceType sub)` 메서드: `MainStance = main; SubStance = sub;`
- 04-RESEARCH.md의 "PlayerStanceManager 완전 골격" 코드 예시를 그대로 사용

**3. SkillBase.cs 수정** — 기존 `[Header("공통 스킬 설정")]` 블록 바로 아래, `private float _cooldownRemaining;` 바로 위에 다음 2줄 삽입:
```csharp
[Header("태세 배율 (보조 태세용 인스턴스는 0.5 설정)")]
[SerializeField] protected float costMultiplier   = 1f;
[SerializeField] protected float effectMultiplier = 1f;
```
기존 코드는 일체 수정하지 않는다. 필드 추가만 수행.
  </action>
  <verify>
    <automated>grep -n "costMultiplier" Assets/Player/SkillBase.cs && grep -n "effectMultiplier" Assets/Player/SkillBase.cs && grep -n "public enum StanceType" Assets/Player/StanceType.cs && grep -n "class PlayerStanceManager" Assets/Player/PlayerStanceManager.cs && grep -n "Initialize" Assets/Player/PlayerStanceManager.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Player/StanceType.cs 파일에 `public enum StanceType` 문자열이 존재한다
    - Assets/Player/StanceType.cs 파일에 `None  = 0` 및 `Water = 1` 값이 존재한다
    - Assets/Player/PlayerStanceManager.cs 파일에 `public class PlayerStanceManager : MonoBehaviour` 문자열이 존재한다
    - Assets/Player/PlayerStanceManager.cs 파일에 `public void Initialize(IPlayerContext context)` 문자열이 존재한다
    - Assets/Player/PlayerStanceManager.cs 파일에 `private SkillBase[] mainSkills` 및 `private SkillBase[] subSkills` 문자열이 존재한다
    - Assets/Player/PlayerStanceManager.cs 파일에 `GetMainSkill` 및 `GetSubSkill` 메서드가 존재한다
    - Assets/Player/PlayerStanceManager.cs 파일에 `SetStances(StanceType main, StanceType sub)` 메서드가 존재한다
    - Assets/Player/SkillBase.cs 파일에 `protected float costMultiplier   = 1f` 문자열이 존재한다
    - Assets/Player/SkillBase.cs 파일에 `protected float effectMultiplier = 1f` 문자열이 존재한다
    - SkillBase.cs의 기존 코드(Initialize, TryUse, ExecuteSkill 등)가 변경되지 않았다
  </acceptance_criteria>
  <done>StanceType enum, PlayerStanceManager 컴포넌트, SkillBase 배율 필드가 모두 존재하며 grep으로 확인 가능</done>
</task>

<task type="auto">
  <name>Task 2: IPlayerContext 확장 + PlayerController 연동</name>
  <files>Assets/Player/IPlayerContext.cs, Assets/Player/PlayerController.cs</files>
  <read_first>
    - Assets/Player/IPlayerContext.cs (현재 인터페이스 전체 구조)
    - Assets/Player/PlayerController.cs (Awake, Start 메서드 전체)
    - Assets/Player/PlayerStanceManager.cs (Task 1에서 생성한 파일 — Initialize 시그니처 확인)
    - .planning/phases/04-player-stance-system/04-RESEARCH.md (Pattern 5, 함정 2 확인)
  </read_first>
  <action>
**1. IPlayerContext.cs 수정** — `void SetInvincible(bool value);` 줄 바로 아래에 1줄 추가:
```csharp
PlayerStanceManager StanceManager { get; }
```
기존 줄은 일체 수정하지 않는다.

**2. PlayerController.cs 수정** — 3곳 수정:

**2a. IPlayerContext 프로퍼티 추가** — 기존 `public PlayerState CurrentState` 프로퍼티 블록 바로 아래에 추가:
```csharp
public PlayerStanceManager StanceManager { get; private set; }
```

**2b. Awake() 수정** — `TryGetComponent(out animator);` 줄 바로 아래에 1줄 추가:
```csharp
StanceManager = GetComponent<PlayerStanceManager>();
```
주의: GetComponent는 PlayerStanceManager가 없으면 null을 반환한다 (Phase 4 Inspector 설정 전까지 정상).

**2c. Start() 수정** — `InitSkill(projectile);` 줄 바로 아래, `inputHandler.OnMove` 이벤트 구독 줄 바로 위에 1줄 추가:
```csharp
StanceManager?.Initialize(this);
```
`?.` null-conditional 연산자 사용 — PlayerStanceManager 컴포넌트가 아직 붙어있지 않아도 NullRef 발생하지 않는다.

기존 코드(Awake의 GetComponent 호출들, Start의 InitSkill 호출들, 이벤트 구독)는 일체 수정하지 않는다.
  </action>
  <verify>
    <automated>grep -n "PlayerStanceManager StanceManager" Assets/Player/IPlayerContext.cs && grep -n "PlayerStanceManager StanceManager" Assets/Player/PlayerController.cs && grep -n "GetComponent<PlayerStanceManager>" Assets/Player/PlayerController.cs && grep -n "StanceManager?.Initialize" Assets/Player/PlayerController.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Player/IPlayerContext.cs에 `PlayerStanceManager StanceManager { get; }` 문자열이 존재한다
    - Assets/Player/PlayerController.cs에 `public PlayerStanceManager StanceManager { get; private set; }` 문자열이 존재한다
    - Assets/Player/PlayerController.cs의 Awake()에 `StanceManager = GetComponent<PlayerStanceManager>();` 문자열이 존재한다
    - Assets/Player/PlayerController.cs의 Start()에 `StanceManager?.Initialize(this);` 문자열이 존재한다
    - IPlayerContext.cs의 기존 프로퍼티(FacingRight, Rigidbody, Stats, CurrentState, ChangeState, SetInvincible)가 변경되지 않았다
    - PlayerController.cs의 기존 InitSkill 3개 호출(basicAttack, wideSlash, projectile)이 그대로 유지된다
  </acceptance_criteria>
  <done>IPlayerContext에 StanceManager 프로퍼티 추가, PlayerController에서 GetComponent + Initialize 호출 추가, 기존 코드 무변경</done>
</task>

</tasks>

<verification>
- `grep -rn "costMultiplier\|effectMultiplier" Assets/Player/SkillBase.cs` — 두 필드 모두 존재
- `grep -rn "public enum StanceType" Assets/Player/StanceType.cs` — enum 존재
- `grep -rn "class PlayerStanceManager" Assets/Player/PlayerStanceManager.cs` — 클래스 존재
- `grep -rn "PlayerStanceManager StanceManager" Assets/Player/IPlayerContext.cs Assets/Player/PlayerController.cs` — 양쪽 모두 존재
- `grep -rn "StanceManager?.Initialize" Assets/Player/PlayerController.cs` — 보조 스킬 초기화 호출 존재
- Unity 편집기에서 컴파일 오류 없음 (수동 확인)
</verification>

<success_criteria>
1. Assets/Player/StanceType.cs 파일이 존재하고 None=0, Water=1 enum 값을 포함한다
2. Assets/Player/PlayerStanceManager.cs 파일이 존재하고 MainStance/SubStance 프로퍼티, mainSkills[3]/subSkills[3] 배열, Initialize/GetMainSkill/GetSubSkill/SetStances 메서드를 포함한다
3. Assets/Player/SkillBase.cs에 costMultiplier=1f, effectMultiplier=1f 필드가 추가되었다
4. Assets/Player/IPlayerContext.cs에 PlayerStanceManager StanceManager 프로퍼티가 추가되었다
5. Assets/Player/PlayerController.cs에서 Awake()에서 GetComponent, Start()에서 Initialize 호출이 추가되었다
6. 기존 코드(SkillBase의 Initialize/TryUse, PlayerController의 기존 InitSkill 호출 등)가 변경되지 않았다
</success_criteria>

<output>
After completion, create `.planning/phases/04-player-stance-system/04-01-SUMMARY.md`
</output>
