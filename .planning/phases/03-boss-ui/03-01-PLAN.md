---
phase: 03-boss-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Assets/Enemy/BossStats.cs, Assets/Enemy/EnemyWorldSpaceUI.cs, Assets/UI/Boss/BossHUDBar.cs]
autonomous: true
requirements: [BOSS-01, BOSS-04, BOSS-05]
must_haves:
  truths:
    - "BossStats 클래스가 EnemyStats를 상속받으며 bossName 필드를 가진다."
    - "일반 적 UI(EnemyWorldSpaceUI)가 보스 오브젝트에서는 자동으로 비활성화된다."
    - "BossHUDBar가 보스의 오염도와 정화율 데이터를 실시간으로 수신한다."
  artifacts:
    - path: "Assets/Enemy/BossStats.cs"
      provides: "보스 전용 데이터 구조"
    - path: "Assets/UI/Boss/BossHUDBar.cs"
      provides: "보스 HUD 컨트롤러 로직"
  key_links:
    - from: "Assets/Enemy/EnemyWorldSpaceUI.cs"
      to: "Assets/Enemy/BossStats.cs"
      via: "Awake() 내 GetComponentInParent 체크"
---

<objective>
보스 UI 구현을 위한 핵심 데이터 클래스와 컴포넌트 로직을 구축합니다.
보스 전용 스탯 클래스를 생성하고, 보스전 시 일반 월드 스페이스 UI가 나타나지 않도록 억제하며, 화면 우측 HUD 바의 업데이트 로직을 구현합니다.
</objective>

<execution_context>
@C:/Users/MSI/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-boss-ui/03-CONTEXT.md
@.planning/phases/03-boss-ui/03-RESEARCH.md
@Assets/Enemy/EnemyStats.cs
@Assets/Enemy/EnemyWorldSpaceUI.cs
@Assets/Player/PlayerHUDBar.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BossStats 클래스 구현</name>
  <files>Assets/Enemy/BossStats.cs</files>
  <behavior>
    - EnemyStats를 상속받아야 함.
    - [SerializeField] string bossName 필드와 공용 Getter를 가짐.
    - IsInPurificationRange 프로퍼티를 구현하여 현재 오염 비율이 정화 가능 구간(Sweet Spot)에 있는지 실시간으로 반환함.
  </behavior>
  <action>
    - Assets/Enemy/BossStats.cs 파일을 생성합니다.
    - EnemyStats를 상속받고 bossName 필드를 추가합니다 (D-BOSS-04).
    - IsInPurificationRange 프로퍼티를 작성하여 basePurificationMin/Max 및 bonusPurificationMargin을 고려한 논리값을 반환하게 합니다.
  </action>
  <verify>
    <automated>코드 컴파일 확인 및 BossStats 클래스가 EnemyStats를 상속하는지 검사</automated>
  </verify>
  <done>BossStats 클래스가 정의되고 bossName 및 정화 구간 확인 로직이 포함됨</done>
</task>

<task type="auto">
  <name>Task 2: EnemyWorldSpaceUI 보스전 억제 로직 추가</name>
  <files>Assets/Enemy/EnemyWorldSpaceUI.cs</files>
  <action>
    - Assets/Enemy/EnemyWorldSpaceUI.cs의 Awake() 메서드를 수정합니다 (D-BOSS-05).
    - GetComponentInParent&lt;BossStats&gt;()를 호출하여 보스 컴포넌트가 존재하면 gameObject.SetActive(false)를 수행하고 즉시 return합니다.
  </action>
  <verify>
    <automated>EnemyWorldSpaceUI.cs 파일 내에 BossStats 체크 로직이 포함되었는지 확인</automated>
  </verify>
  <done>보스에게 부착된 일반 월드 스페이스 UI가 런타임에 자동으로 비활성화됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BossHUDBar 컨트롤러 스크립트 구현</name>
  <files>Assets/UI/Boss/BossHUDBar.cs</files>
  <behavior>
    - BossStats의 이벤트를 구독하여 UI를 갱신함.
    - 컨테이너 높이는 CorruptionRatio에 따라 localScale.y를 조절함 (D-BOSS-02).
    - 내부 정화 게이지는 HP가 아닌 (MaxCorruption - CurrentCorruption) / MaxCorruption 비율로 차오르게 하거나, 기획 의도에 맞게 HP 비율을 표시함 (연구 결과에 따라 정화율 표시).
    - 보스가 Sweet Spot에 진입하면 게이지 이미지가 펄스(Glow) 효과를 내도록 Update에서 처리함 (D-BOSS-08).
  </behavior>
  <action>
    - Assets/UI/Boss/BossHUDBar.cs를 생성합니다.
    - PlayerHUDBar.cs의 로직을 기반으로 하되, 보스 전용 데이터 바인딩 로직을 작성합니다.
    - containerRect 스케일링 로직을 CorruptionRatio에 연결합니다.
    - Update()에서 bossStats.IsInPurificationRange가 true일 때 컬러 펄스 효과를 적용합니다.
  </action>
  <verify>
    <automated>BossHUDBar.cs가 컴파일 오류 없이 생성되었는지 확인</automated>
  </verify>
  <done>보스 UI의 데이터 연동 및 시각적 피드백(수축, 펄스) 로직 구현 완료</done>
</task>

</tasks>

<verification>
- 모든 스크립트의 컴파일 오류 여부 확인.
- BossStats가 EnemyStats의 이벤트를 정상적으로 상속받아 BossHUDBar에서 수신 가능한지 확인.
</verification>

<success_criteria>
- BossStats 클래스 생성 완료.
- EnemyWorldSpaceUI 보스 억제 로직 반영 완료.
- BossHUDBar 실시간 업데이트 및 Sweet Spot 피드백 로직 구현 완료.
</success_criteria>

<output>
After completion, create `.planning/phases/03-boss-ui/03-01-SUMMARY.md`
</output>
