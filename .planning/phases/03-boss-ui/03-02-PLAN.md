---
phase: 03-boss-ui
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [Assets/UI/Boss/BossUIManager.cs, Assets/Enemy/BossRoomTrigger.cs]
autonomous: true
requirements: [BOSS-03]
must_haves:
  truths:
    - "BossUIManager가 싱글톤으로 동작하며 보스 UI를 제어한다."
    - "BossRoomTrigger가 플레이어를 감지하여 보스 UI 활성화 요청을 보낸다."
  artifacts:
    - path: "Assets/UI/Boss/BossUIManager.cs"
      provides: "보스 UI 생명주기 관리"
    - path: "Assets/Enemy/BossRoomTrigger.cs"
      provides: "보스전 진입 감지"
---

<objective>
보스 UI의 활성화, 비활성화 및 화면 연출(Fade In/Out)을 관리하는 시스템을 구축합니다.
보스 방 진입 시 UI를 트리거하고, 보스 처치 시 자동으로 UI가 사라지도록 연결합니다.
</objective>

<execution_context>
@C:/Users/MSI/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-boss-ui/03-CONTEXT.md
@.planning/phases/03-boss-ui/03-RESEARCH.md
@Assets/UI/Boss/BossHUDBar.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BossUIManager 클래스 구현</name>
  <files>Assets/UI/Boss/BossUIManager.cs</files>
  <behavior>
    - Singleton 패턴으로 구현됨.
    - [SerializeField] CanvasGroup bossUICanvasGroup 필드를 가짐 (Fade용).
    - [SerializeField] BossHUDBar bossHUDBar 필드를 가짐.
    - ShowBossUI(BossStats boss)와 HideBossUI() 메서드를 제공함.
    - ShowBossUI 시 CanvasGroup.alpha를 1로 페이드하고 bossHUDBar를 해당 보스 데이터에 바인딩함 (D-BOSS-07, D-BOSS-10).
    - 보스 사망(OnDeath) 시 자동으로 HideBossUI를 트리거함.
  </behavior>
  <action>
    - Assets/UI/Boss/BossUIManager.cs를 생성합니다.
    - CanvasGroup을 이용한 투명도 제어 로직을 작성합니다.
    - 보스 스탯 변화 이벤트를 구독하여 UI를 표시하고 해제하는 라이프사이클을 구현합니다.
  </action>
  <verify>
    <automated>BossUIManager.cs 컴파일 확인 및 Show/Hide 메서드 존재 확인</automated>
  </verify>
  <done>보스 UI의 중앙 관리 및 페이드 효과 제어 로직 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: BossRoomTrigger 구현</name>
  <files>Assets/Enemy/BossRoomTrigger.cs</files>
  <action>
    - Assets/Enemy/BossRoomTrigger.cs를 생성합니다.
    - BoxCollider2D(isTrigger)와 연결되어 플레이어 태그 감지 시 BossUIManager.Instance.ShowBossUI(bossStats)를 호출합니다 (D-BOSS-06).
    - 한 번 발동 후에는 더 이상 트리거되지 않도록 처리합니다.
  </action>
  <verify>
    <automated>BossRoomTrigger.cs 컴파일 및 OnTriggerEnter2D 로직 포함 확인</automated>
  </verify>
  <done>보스 구역 진입 시 UI 활성화 요청이 정상적으로 발생함</done>
</task>

</tasks>

<verification>
- BossUIManager와 BossRoomTrigger의 상호작용 로직 검사.
- 컴파일 오류 및 이벤트 구독/해제 시점 누락 여부 확인.
</verification>

<success_criteria>
- BossUIManager 싱글톤 및 페이드 시스템 구현 완료.
- BossRoomTrigger 진입 감지 로직 구현 완료.
</success_criteria>

<output>
After completion, create `.planning/phases/03-boss-ui/03-02-SUMMARY.md`
</output>
