---
phase: 02-enemy-world-space-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Enemy/EnemyStats.cs
  - .planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md
autonomous: true
requirements:
  - TECH-02
  - ENM-03
deferred:
  - ENM-06
must_haves:
  truths:
    - "EnemyStats exposes OnHpChanged(float current, float max) event that fires whenever CurrentHp changes"
    - "EnemyStats exposes OnCorruptionChanged(float current, float max) event that fires whenever CurrentCorruption changes"
    - "Both events fire inside TakeDamage() after field updates, and OnHpChanged also fires inside SpendHpOnAttack() after HP is decremented"
    - "Existing OnDamaged and OnDeath events remain unchanged and still fire in the same order as before"
    - "Unity Editor compiles with zero errors after changes"
  artifacts:
    - path: "Assets/Enemy/EnemyStats.cs"
      provides: "OnHpChanged and OnCorruptionChanged Action<float,float> events"
      contains: "public event Action<float, float> OnHpChanged"
    - path: ".planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md"
      provides: "Wave 0 manual UAT script for Phase 2 (7 behaviors)"
      contains: "Sweet Spot"
  key_links:
    - from: "EnemyStats.TakeDamage"
      to: "OnHpChanged subscribers"
      via: "event invoke after CurrentHp -= hpDamage"
      pattern: "OnHpChanged\\?\\.Invoke\\(CurrentHp, maxHp\\)"
    - from: "EnemyStats.TakeDamage"
      to: "OnCorruptionChanged subscribers"
      via: "event invoke after CurrentCorruption -= corruptionDamage"
      pattern: "OnCorruptionChanged\\?\\.Invoke\\(CurrentCorruption, maxCorruption\\)"
    - from: "EnemyStats.SpendHpOnAttack"
      to: "OnHpChanged subscribers"
      via: "event invoke after CurrentHp -= cost (before Die check)"
      pattern: "OnHpChanged\\?\\.Invoke"
---

<objective>
Add TECH-02 (`OnHpChanged`) and its sibling `OnCorruptionChanged` events to `EnemyStats.cs` so that UI bars (built in Plan 02) can subscribe via Bind/Unbind pattern. Also create the Wave 0 manual UAT script that Plan 03 will run to verify all Phase 2 behaviors.

Purpose: EnemyStats currently only exposes `OnDamaged` (no value payload) and `OnDeath`. The UI needs explicit (current, max) payloads so bars can update fill ratios deterministically. Without these events, no UI code can subscribe. This is the foundation wave.

Output:
- Modified `EnemyStats.cs` with two new Action<float, float> events wired into `TakeDamage()` and `SpendHpOnAttack()`.
- `02-HUMAN-UAT.md` with 7 manual Play Mode tests matching the Phase 1 UAT format.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-enemy-world-space-ui/02-CONTEXT.md
@.planning/phases/02-enemy-world-space-ui/02-RESEARCH.md
@.planning/phases/02-enemy-world-space-ui/02-UI-SPEC.md
@.planning/phases/02-enemy-world-space-ui/02-VALIDATION.md
@Assets/Enemy/EnemyStats.cs
@.planning/phases/01-player-hud/01-HUMAN-UAT.md

<interfaces>
Current EnemyStats.cs public surface (what exists today):

```csharp
public class EnemyStats : MonoBehaviour, IDamageable
{
    public float basePurificationMin     = 0.3f;
    public float basePurificationMax     = 0.7f;
    public float bonusPurificationMargin = 0f;

    public float CurrentHp         { get; private set; }
    public float CurrentCorruption { get; private set; }
    public float MaxHp             => maxHp;
    public float MaxCorruption     => maxCorruption;
    public float CorruptionRatio   => CurrentCorruption / maxCorruption;

    // Existing events (keep these unchanged)
    public event Action OnDamaged;
    public event Action OnDeath;

    public void TakeDamage(float hpDamage, float corruptionDamage);
    public bool SpendHpOnAttack(float cost);
    public void WidenPurificationRange(float amount);
    public void Die();
}
```

NOTE: `OnCorruptionChanged` does NOT exist today. This plan adds it. Both new events use `System.Action<float, float>` signature (already imported via `using System;` at top of file, line 1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add OnHpChanged and OnCorruptionChanged events to EnemyStats.cs (per D-11, TECH-02)</name>
  <files>Assets/Enemy/EnemyStats.cs</files>
  <read_first>
    - Assets/Enemy/EnemyStats.cs (current implementation — verify line numbers of TakeDamage and SpendHpOnAttack before editing)
    - Assets/Player/PlayerHUDBar.cs (reference pattern: how Phase 1 subscribes to PlayerWaterStats events)
    - .planning/phases/02-enemy-world-space-ui/02-RESEARCH.md (TECH-02 add location specification)
    - .planning/phases/02-enemy-world-space-ui/02-CONTEXT.md (D-11, D-12)
  </read_first>
  <behavior>
    - Test 1: After TakeDamage(10, 5), OnHpChanged fires once with args (CurrentHp after decrement, MaxHp) and OnCorruptionChanged fires once with args (CurrentCorruption after decrement, MaxCorruption).
    - Test 2: After SpendHpOnAttack(20) where enemy survives, OnHpChanged fires once with (CurrentHp - 20, MaxHp).
    - Test 3: OnDamaged and OnDeath still fire in the same order and conditions as before (no regression).
  </behavior>
  <action>
    Edit `Assets/Enemy/EnemyStats.cs` to add two new events and wire them into state mutations.

    STEP 1 — Add event declarations in the `// ─── 이벤트 ──────────────────────` region, AFTER the existing `OnDeath` event declaration:

    ```csharp
    /// HP 변경 시 발생 (current, max). UI 바가 구독한다. TECH-02.
    public event Action<float, float> OnHpChanged;

    /// 오염도 변경 시 발생 (current, max). UI 바가 구독한다.
    public event Action<float, float> OnCorruptionChanged;
    ```

    STEP 2 — Modify `TakeDamage(float hpDamage, float corruptionDamage)` method. AFTER the existing `CurrentCorruption -= corruptionDamage;` line and BEFORE the existing `Debug.Log(...)` call, insert the two event invocations:

    ```csharp
    OnHpChanged?.Invoke(CurrentHp, maxHp);
    OnCorruptionChanged?.Invoke(CurrentCorruption, maxCorruption);
    ```

    The existing `OnDamaged?.Invoke();` call (currently after the Debug.Log) MUST remain unchanged in its current position. The existing `if (CurrentHp <= 0f) CheckDeathState();` at the end of the method MUST remain unchanged.

    Final TakeDamage method body ordering (do not deviate):
    1. `if (isDead) return;`
    2. `CurrentHp -= hpDamage;`
    3. `CurrentCorruption -= corruptionDamage;`
    4. `OnHpChanged?.Invoke(CurrentHp, maxHp);`  ← NEW
    5. `OnCorruptionChanged?.Invoke(CurrentCorruption, maxCorruption);`  ← NEW
    6. `Debug.Log(...)` (existing, unchanged)
    7. `OnDamaged?.Invoke();` (existing, unchanged)
    8. `if (CurrentHp <= 0f) CheckDeathState();` (existing, unchanged)

    STEP 3 — Modify `SpendHpOnAttack(float cost)` method. AFTER the existing `CurrentHp -= cost;` line and BEFORE the existing `if (CurrentHp <= 0f)` check, insert:

    ```csharp
    OnHpChanged?.Invoke(CurrentHp, maxHp);
    ```

    Do NOT fire OnCorruptionChanged here — this method does not modify corruption.

    STEP 4 — Do NOT modify:
    - `Awake()` — current values are set, no events fired at startup (subscribers use explicit Bind() to pull initial state, per D-12)
    - `Purify()`, `Die()`, `CheckDeathState()` — no changes needed
    - Any field declarations, properties, or the `#if UNITY_EDITOR OnGUI` block

    STEP 5 — Verify `using System;` is already at the top of the file (it is — line 1). Action<float, float> requires this namespace.

    Rationale: events fire AFTER state mutation but BEFORE Debug.Log and OnDamaged, so subscribers see the new values and ordering stays predictable. Per D-11 and TECH-02, these are the only two event additions.
  </action>
  <verify>
    <automated>cd "C:/Users/MSI/Projeect_A.E/My project" && grep -c "public event Action<float, float> OnHpChanged" Assets/Enemy/EnemyStats.cs && grep -c "public event Action<float, float> OnCorruptionChanged" Assets/Enemy/EnemyStats.cs && grep -c "OnHpChanged?.Invoke(CurrentHp, maxHp)" Assets/Enemy/EnemyStats.cs && grep -c "OnCorruptionChanged?.Invoke(CurrentCorruption, maxCorruption)" Assets/Enemy/EnemyStats.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public event Action<float, float> OnHpChanged" Assets/Enemy/EnemyStats.cs` returns 1
    - `grep -c "public event Action<float, float> OnCorruptionChanged" Assets/Enemy/EnemyStats.cs` returns 1
    - `grep -c "OnHpChanged?.Invoke(CurrentHp, maxHp)" Assets/Enemy/EnemyStats.cs` returns exactly 2 (one in TakeDamage, one in SpendHpOnAttack)
    - `grep -c "OnCorruptionChanged?.Invoke(CurrentCorruption, maxCorruption)" Assets/Enemy/EnemyStats.cs` returns exactly 1 (only in TakeDamage)
    - `grep -c "public event Action OnDamaged" Assets/Enemy/EnemyStats.cs` still returns 1 (existing event preserved)
    - `grep -c "public event Action OnDeath" Assets/Enemy/EnemyStats.cs` still returns 1 (existing event preserved)
    - Opening Unity Editor shows zero compile errors in Console (human-verified at wave end)
  </acceptance_criteria>
  <done>
    EnemyStats.cs compiles with two new events (OnHpChanged, OnCorruptionChanged), events fire at the correct locations in TakeDamage and SpendHpOnAttack, no existing behavior is altered.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 02-HUMAN-UAT.md Wave 0 manual test script</name>
  <files>.planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/01-player-hud/01-HUMAN-UAT.md (reference format for Wave 0 UAT)
    - .planning/phases/02-enemy-world-space-ui/02-VALIDATION.md (lists the 7 behaviors and their requirement IDs)
    - .planning/phases/02-enemy-world-space-ui/02-UI-SPEC.md (section "Interaction & Animation Contract" — fade-in 0.15s, OnDeath immediate SetActive(false))
  </read_first>
  <action>
    Create `.planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md` matching the Phase 1 UAT format.

    File MUST contain this exact frontmatter:

    ```
    ---
    status: partial
    phase: 02-enemy-world-space-ui
    source: [02-VALIDATION.md]
    started: 2026-04-06T00:00:00Z
    updated: 2026-04-06T00:00:00Z
    ---
    ```

    Followed by a `## Current Test` section containing `[awaiting human testing]`, then `## Tests` containing these 7 numbered tests. Each test MUST have an `expected:` line and a `result: [pending]` line.

    1. **Unity Editor compiles with zero errors** — expected: EnemyStats.cs and EnemyWorldSpaceUI.cs compile cleanly with zero errors in Console. Requirement: TECH-02.
    2. **HP bar follows moving enemy** — expected: In Play Mode, move enemy across the scene; HP bar stays anchored above the enemy sprite head (no lag, no detachment). Requirement: ENM-01.
    3. **Bars hidden before first hit, visible after** — expected: At scene start, HP/Corruption bars are invisible (CanvasGroup.alpha == 0). After first damage via player attack, bars fade in over ~0.15s. Requirement: ENM-02.
    4. **Corruption bar updates in real-time** — expected: Trigger corruption change (player attack); Corruption bar fillAmount matches CurrentCorruption / MaxCorruption. Requirement: ENM-03.
    5. **Sweet Spot overlay at correct fraction** — expected: Pause Play Mode, select enemy in hierarchy, inspect Sweet Spot overlay RectTransform: anchorMin.x equals Clamp01(basePurificationMin - bonusPurificationMargin) and anchorMax.x equals Clamp01(basePurificationMax + bonusPurificationMargin). offsetMin and offsetMax are both (0,0). Requirement: ENM-04.
    6. **Different enemy types show different Sweet Spot ranges** — expected: Place two enemies with different basePurificationMin/Max values in scene; confirm overlays visually span different widths/positions. Requirement: ENM-05.
    7. **No event leak on Purify path (TECH-03)** — expected: Lower enemy HP to 0 within purification range so Purify() runs → PurifiedNPC.Activate() is called → EnemyStats.enabled = false → No NullReferenceException or MissingReferenceException appears in the Unity Console. The UI Canvas goes inactive on OnDeath. Requirement: TECH-03.

    After the 7 tests, add a `## Deferred` section with the text: "ENM-06 (PURIFIED/DESTROYED popup text) is deferred per CONTEXT.md D-07 and is NOT tested in this phase."

    Do NOT fill in any `result:` values — leave all as `[pending]`. This is the test script, not the test report.
  </action>
  <verify>
    <automated>cd "C:/Users/MSI/Projeect_A.E/My project" && test -f .planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md && grep -c "result: \[pending\]" .planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md && grep -c "Sweet Spot" .planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md && grep -c "TECH-03" .planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md && grep -c "ENM-06" .planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-enemy-world-space-ui/02-HUMAN-UAT.md` exists
    - `grep -c "result: \[pending\]" 02-HUMAN-UAT.md` returns 7 (one per test)
    - `grep -c "^### " 02-HUMAN-UAT.md` returns 7 (seven numbered test headings)
    - File contains literal strings: "ENM-01", "ENM-02", "ENM-03", "ENM-04", "ENM-05", "TECH-02", "TECH-03" (all 7 requirement IDs)
    - File contains literal string "ENM-06" only inside the Deferred section
    - Frontmatter contains `status: partial` and `phase: 02-enemy-world-space-ui`
  </acceptance_criteria>
  <done>
    02-HUMAN-UAT.md exists with all 7 pending tests covering TECH-02, ENM-01..05, TECH-03 and an explicit deferred note for ENM-06, ready for Plan 03 checkpoint to execute.
  </done>
</task>

</tasks>

<verification>
1. Open Unity Editor after Task 1 — Console shows zero compile errors.
2. All grep acceptance criteria for both tasks pass from the repo root.
3. File 02-HUMAN-UAT.md exists and has `status: partial` frontmatter.
</verification>

<success_criteria>
- EnemyStats.cs exposes OnHpChanged and OnCorruptionChanged events with correct signatures.
- Events fire at the correct code locations (verified by grep count).
- Unity Editor compiles cleanly.
- 02-HUMAN-UAT.md exists with 7 pending tests and ENM-06 deferred note.
- Existing OnDamaged and OnDeath events preserved unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-enemy-world-space-ui/02-01-SUMMARY.md` documenting:
- Exact line numbers where OnHpChanged and OnCorruptionChanged events were added.
- Verification that TakeDamage ordering matches the 8-step sequence in the action.
- Confirmation that Unity compiles with zero errors.
- Confirmation that 02-HUMAN-UAT.md has 7 pending tests.
</output>
